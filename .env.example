# Server
PORT=3000
NODE_ENV=development

# Frontend URL (for CORS and password reset emails)
FRONTEND_URL=http://localhost:9999

# Docker Compose (when using docker-compose.yml with GHCR images)
# OPCS_IMAGE_PREFIX=ghcr.io/your-org/opcs-onboarding-system
# OPCS_IMAGE_TAG=latest
# FRONTEND_PORT=80
# BACKEND_PORT=3000

# Session
SESSION_SECRET=your-super-secret-session-key-change-in-production

# Cookies: set SECURE_COOKIES=true when serving over HTTPS
# SECURE_COOKIES=false

# Google Drive (optional - for document storage)
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
GOOGLE_DRIVE_FOLDER_ID=

# Encryption (optional; if unset, a key can be generated and stored in DB on first use)
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# User accounts (including admin) are created by signing up with email; the first user
# becomes admin. Passwords are set during initial onboarding (password setup flow), not here.
